new Swiper('.slider', {
    // direction: 'vertical',  // Для вертикального скрола
    speed: 2400,
    mousewheel: {
        enabled: true,
        sensitivity: 2.4
    },
    spaceBetween: 18,
    parallax: true,
    // freeMode: true,
})